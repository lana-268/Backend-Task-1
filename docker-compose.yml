services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: eventify
      POSTGRES_PASSWORD: eventify
      POSTGRES_DB: eventify
    ports:
      - "5432:5432"
    volumes:
      - eventify-postgres:/var/lib/postgresql/data

volumes:
  eventify-postgres:
